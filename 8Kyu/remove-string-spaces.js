//Write a function that removes the spaces from the string, then return the resultant string.


//My Solution
function noSpace(x){
    //split removes what is between the ''.
    //join combines the strings into a new array.
    return x = x.split(' ').join('');
  }